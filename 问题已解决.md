# ✅ 问题已解决！

## 🔍 问题根源

**配置文件路径错误！**

- ❌ 错误路径：`src/resources/application.yml`
- ✅ 正确路径：`src/main/resources/application.yml`

Maven 标准的资源文件目录是 `src/main/resources`，而不是 `src/resources`。

这就是为什么 Maven 日志显示：
```
[INFO] skip non existing resourceDirectory D:\--main\user-service\src\main\resources
```

因为配置文件不在正确的位置，Spring Boot 无法读取数据库配置，导致启动失败。

## ✅ 已完成的修复

我已经将所有服务的配置文件移动到正确位置：

1. ✅ `user-service/src/main/resources/application.yml`
2. ✅ `product-service/src/main/resources/application.yml`
3. ✅ `order-service/src/main/resources/application.yml`
4. ✅ `gateway-service/src/main/resources/application.yml`

旧的 `src/resources` 目录已删除。

## 🚀 现在请立即重新启动服务

### 在 IntelliJ IDEA 中：

1. **停止所有正在运行的服务**
   - 点击 Run 窗口中的红色停止按钮 ⏹️

2. **启动 user-service**
   - Maven 窗口 → user-service → Plugins → spring-boot → 双击 `spring-boot:run`
   - 等待启动完成

3. **启动 product-service**
   - Maven 窗口 → product-service → Plugins → spring-boot → 双击 `spring-boot:run`
   - 等待启动完成

4. **启动 order-service**
   - Maven 窗口 → order-service → Plugins → spring-boot → 双击 `spring-boot:run`
   - 等待启动完成

5. **重新启动 gateway-service**（如果还在运行就不用）
   - Maven 窗口 → gateway-service → Plugins → spring-boot → 双击 `spring-boot:run`

## ✅ 预期结果

现在每个服务启动时，你应该看到：

```
[INFO] --- resources:3.3.1:resources (default-resources) @ user-service ---
[INFO] Copying 1 resource from src\main\resources to target\classes  ← 注意这一行！
[INFO] 
[INFO] --- compiler:3.13.0:compile (default-compile) @ user-service ---
[INFO] Nothing to compile - all classes are up to date.
...
2025-12-28 ... INFO ... : HikariPool-1 - Starting...
2025-12-28 ... INFO ... : HikariPool-1 - Start completed.
2025-12-28 ... INFO ... : Tomcat started on port 8081 (http)
2025-12-28 ... INFO ... : Started UserServiceApplication in X.XXX seconds
```

**关键成功标志**：
- ✅ `Copying 1 resource from src\main\resources to target\classes` - 配置文件被正确复制
- ✅ `HikariPool-1 - Start completed` - 数据库连接成功
- ✅ `Started UserServiceApplication in X.XXX seconds` - 启动成功

## 🎯 验证所有服务

### 1. 检查端口
```powershell
netstat -ano | findstr "8080 8081 8082 8083"
```

应该看到 4 个端口都在 LISTENING 状态。

### 2. 访问 Nacos
打开浏览器：http://localhost:8848/nacos
- 用户名：nacos
- 密码：nacos

在 **服务管理 > 服务列表** 中应该看到 4 个服务：
- ✅ user-service
- ✅ product-service
- ✅ order-service
- ✅ gateway-service

### 3. 测试 API
```powershell
# 创建用户
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/users" -Method POST -ContentType "application/json" -Body '{"username":"张三","email":"zhangsan@example.com"}'

# 查询用户
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/users"

# 创建产品
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/products" -Method POST -ContentType "application/json" -Body '{"name":"iPhone 15","price":5999,"stock":100}'

# 查询产品
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/products"

# 创建订单
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/orders" -Method POST -ContentType "application/json" -Body '{"userId":1,"productId":1,"quantity":2}'

# 查询订单
Invoke-WebRequest -Uri "http://localhost:8080/api/v1/orders/1"
```

## 🎉 恭喜！

所有问题都已解决！现在你的微服务项目应该可以正常运行了。

如果还有任何问题，请告诉我！🚀
