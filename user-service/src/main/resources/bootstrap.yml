spring:
  application:
    name: user-service
  cloud:
    nacos:
      config:
        server-addr: nacos:8848
        file-extension: yaml
        namespace: # 使用默认命名空间
        group: DEFAULT_GROUP
        refresh-enabled: true  # 启用动态刷新
  profiles:
    active: dev
