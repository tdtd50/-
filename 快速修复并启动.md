# 🚀 快速修复并启动指南

## ✅ 已完成的修复

我已经为你修复了以下问题：

1. ✅ **Java 版本不匹配** - 从 Java 25 改为 Java 21
2. ✅ **依赖缺失** - 为所有服务添加了 spring-boot-starter
3. ✅ **IDEA 配置错误** - 更新了项目 JDK 配置
4. ✅ **清理编译缓存** - 删除了所有 target 目录

## 📋 现在只需 3 步即可启动

### 第 1 步：在 IntelliJ IDEA 中重新加载项目

**方式 A（推荐）：**
1. 点击右侧的 **Maven** 工具窗口（或按 `Alt+M`）
2. 点击顶部的刷新按钮 🔄 (**Reload All Maven Projects**)
3. 等待底部进度条完成（可能需要 1-3 分钟下载依赖）

**方式 B（如果方式 A 不行）：**
1. 菜单栏：**File** → **Invalidate Caches / Restart**
2. 选择 **Invalidate and Restart**
3. 等待 IDEA 重启完成

### 第 2 步：验证 JDK 配置

1. 按 `Ctrl+Alt+Shift+S` 打开 **Project Structure**
2. 在 **Project** 标签页检查：
   - **SDK**: 应该是 **21** (Temurin-21.0.4)
   - **Language level**: 应该是 **21**
3. 如果不对，请选择正确的 JDK 21
4. 点击 **OK**

### 第 3 步：启动所有服务

1. 在 IDEA 右上角找到运行配置下拉菜单
2. 选择 **"All Services"**
3. 点击绿色运行按钮 ▶️

## 🎯 预期结果

启动成功后，你应该看到：

### 控制台输出
```
Started UserServiceApplication in X.XXX seconds
Started ProductServiceApplication in X.XXX seconds
Started OrderServiceApplication in X.XXX seconds
Started GatewayApplication in X.XXX seconds
```

### Nacos 控制台
访问 http://localhost:8848/nacos (用户名/密码: nacos/nacos)

在 **服务管理 > 服务列表** 中应该看到 4 个服务：
- ✅ user-service
- ✅ product-service
- ✅ order-service
- ✅ gateway-service

## 🧪 测试服务

### 测试 1：创建用户
```bash
curl -X POST http://localhost:8080/api/v1/users \
  -H "Content-Type: application/json" \
  -d "{\"username\":\"张三\",\"email\":\"zhangsan@example.com\"}"
```

### 测试 2：查询用户列表
```bash
curl http://localhost:8080/api/v1/users
```

### 测试 3：创建产品
```bash
curl -X POST http://localhost:8080/api/v1/products \
  -H "Content-Type: application/json" \
  -d "{\"name\":\"iPhone 15\",\"price\":5999,\"stock\":100}"
```

### 测试 4：创建订单
```bash
curl -X POST http://localhost:8080/api/v1/orders \
  -H "Content-Type: application/json" \
  -d "{\"userId\":1,\"productId\":1,\"quantity\":2}"
```

## ❓ 如果还有问题

### 问题 1：Maven 依赖下载失败
**解决方案**：
1. 检查网络连接
2. 在 Maven 工具窗口右键点击项目 → **Maven** → **Reimport**
3. 或者删除 `C:\Users\你的用户名\.m2\repository\org\springframework` 后重新下载

### 问题 2：端口被占用
**解决方案**：
```bash
# 检查端口占用
netstat -ano | findstr "8080 8081 8082 8083"

# 结束占用端口的进程
taskkill /PID <进程ID> /F
```

### 问题 3：服务启动失败
**解决方案**：
1. 不要使用 "All Services"，改为逐个启动
2. 先启动 **UserServiceApplication**
3. 查看完整的错误日志
4. 复制错误信息告诉我

### 问题 4：数据库连接失败
**解决方案**：
```bash
# 检查 MySQL 容器状态
docker ps | findstr mysql

# 如果没有运行，启动它
docker-compose up -d mysql

# 重新初始化数据库
docker exec -i mysql mysql -uroot -p123456 -e "CREATE DATABASE IF NOT EXISTS user_db; CREATE DATABASE IF NOT EXISTS product_db; CREATE DATABASE IF NOT EXISTS order_db;"
```

## 📞 需要帮助？

如果按照上述步骤操作后仍有问题，请：
1. 截图完整的错误信息
2. 告诉我具体在哪一步出错
3. 我会继续帮你解决

---

**提示**：第一次启动可能需要较长时间下载依赖，请耐心等待！
